defaults:
  - datasets: data
  - models: model
  - experiments: exp
  - _self_

seed: 42

hydra:
  job_logging:
    version: 1
    root:
      level: WARNING
      handlers: [console, file]
  hydra_logging:
    level: WARNING

results:
  save_path: "reports/"
